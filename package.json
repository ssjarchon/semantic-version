{
  "name": "ts-semantic-version",
  "version": "0.0.3",
  "main": "dist/index.js",
  "files": [
    "dist",
    "!dist/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:types": "tsc -p tsconfig.type-tests.json --noEmit",
    "prepublish": "npm run build"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.9",
    "typescript": "^5.6.3"
  }
}
